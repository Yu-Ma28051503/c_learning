# 変数

num = 335  # 整数
pi = 3.14  # 浮動小数点
name = "Python"  # 文字列
is_ok = True  # 真偽値

print(num, type(num))
print(pi, type(pi))
print(name, type(name))
print(is_ok, type(is_ok))

# global変数はglobal_の後に変数名をつけることで定義
global_num = 10 # グローバル変数
